[tool.poetry]
name = "doc-translator"
version = "0.1.0"
description = "Translate .docx, .pdf, .xlsx files while preserving formatting (best-effort)."
authors = ["crt_"]

[tool.poetry.dependencies]
python = "^3.12.10"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
